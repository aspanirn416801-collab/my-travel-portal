# 旅遊手冊系統 20260917_09 回歸修復與操作型測試指引

本文件詳細記錄 `20260917_09` 版本之架構修正、跨行程快照隔離、Session 狀態一致性維護、BFCache 門禁修復及自動化操作型測試。

---

## 一、本次 20260917_09 修正項目詳細對照表

| 修正項目 | 前一版 (24b1113) 實質缺陷 | 20260917_09 重構修復實作 | 驗證方式與狀態 |
| :--- | :--- | :--- | :--- |
| **P0 跨行程快照隔離** | 快照為單一變數且未綁定 UUID，岡山快照可能在奧捷編輯失敗時串錯還原。 | 建立 `confirmedSnapshots = new Map()`，以 `tripUuid` 為鍵進行物理隔離，還原時嚴格僅取當前行程。 | **通過 (操作型模擬測試)** |
| **P0 Session 失敗同步回滾** | `save()` 送出前過早寫入 Session，儲存失敗未同步復原，重新整理後讀到髒資料。 | 1. 移除連線前過早寫入；<br>2. 僅在雲端確認成功後更新 Session；<br>3. 失敗時以確認快照精準覆蓋 Session。 | **通過 (操作型模擬測試)** |
| **P1 BFCache 瀏覽器上一頁誤鎖** | `pageshow` 仍檢查已淘汰之 `unlocked_trip_` Session 旗標，上一頁返回時訪客與成員被誤鎖。 | 移除舊 Session 旗標檢查，統一呼叫現代門禁函式 `isTripUnlocked(currentTripUuid, hasPassword)`。 | **通過 (操作型模擬測試)** |
| **P1 表單屬性與內容全量跳脫** | 表單 input 僅局部套用，航班時間、飯店、景點說明、代購、交通等仍有 40 多處未跳脫。 | 全面套用 `escapeAttribute()` 於 input/hidden/option，textarea 內容套用 `escapeHtml()`。 | **通過 (屬性覆蓋檢查)** |
| **操作型回歸測試** | 原腳本僅以字串存在性檢驗，缺乏操作型模擬與狀態還原斷言。 | 重構為 [`verify_v09_regression.js`](verify_v09_regression.js)，對快照隔離、回滾、門禁邊界進行真實資料操作與狀態斷言。 | **通過 (12/12 測試通過)** |
| **CDN 與瀏覽器快取升級** | 版本號沿用 `20260917_08`，受限於 GitHub Pages 快取可能讀到舊版。 | `app.js` 與 `index.html` 統一升級為 `20260917_09`，破除瀏覽器舊快取。 | **通過 (檔案校驗)** |

---

## 二、驗收矩陣現狀

| 驗收維度 | 檢驗項目 | 當前狀態 | 說明 |
| :--- | :--- | :--- | :--- |
| **靜態語法檢查** | `node -c app.js; node -c gas-code.js` | **通過** | 無語法錯誤。 |
| **自動化回歸檢測** | `node verify_v09_regression.js` | **12 項全數通過** | 包含跨行程隔離實測、PIN 解鎖失敗回滾實測、BFCache 門禁實測、HTML 屬性防穿透實測等。 |
| **版本標籤一致性** | 檔案版本號核對 | **通過** | `index.html` 與 `app.js` 均為 `20260917_09`。 |
| **公開端行程切換** | 岡山／奧捷行程切換與卡片顯示 | **通過** | 公開端切換正常，未再發生串頁。 |
| **管理員實體登入角色切換** | 實體帳號於瀏覽器登入 | **待使用者登入驗收** | 需以管理員 Google 帳號實體登入，確認角色轉為 `admin` 且右上角出現後台膠囊。 |
| **授權成員免 PIN 進入手冊** | 實體帳號於瀏覽器登入 | **待使用者登入驗收** | 將測試帳號填入主表 `allowed_users`，確認登入後免輸 PIN 直接開啟該行程。 |
| **授權成員修改 7 類內容表** | 實體帳號於瀏覽器登入 | **待使用者登入驗收** | 成員登入後修改清單或景點，確認雲端儲存成功，且 Info 表不受影響。 |
| **管理員 PIN 保持與變更** | 實體帳號於瀏覽器登入 | **待使用者登入驗收** | 於後台測試保持現有 PIN、修改 PIN 與取消 PIN 三種場景。 |
| **圖片上傳至 Google Drive** | 實體帳號於瀏覽器登入 | **待使用者登入驗收** | 於前端上傳照片，確認檔案成功存入 Google Drive 且縮圖正確顯示。 |

---

## 三、如何本機復現驗證

在專案目錄下執行：
```bash
node verify_v09_regression.js
```
該腳本會實際建構模擬狀態環境，對跨行程快照、Session 同步、門禁邊界與跳脫進行操作型斷言。
