# 旅遊手冊系統 20260917_12 正式整合修復與架構驗收指引

本文件詳細記錄 `20260917_12` 版本之核心修正：建立統一門禁轉接函式解決 P0 參數缺漏、分離 URL 協議白名單與 HTML 屬性轉義職責解決 P1 圖片網址破壞、回歸測試套件名實相符調整與線上真實帳號驗收指引。

---

## 一、本次 20260917_12 修正重點詳細對照表

| 修正項目 | 前版 (21ad0cd / _11) 實質缺陷 | 20260917_12 具體重構解法 | 驗證方式與狀態 |
| :--- | :--- | :--- | :--- |
| **P0 門禁整合接線參數缺漏** | `trip-state.js` 模組函式需要 5 個參數，但 `app.js` 正式頁面 8 處呼叫點均只傳 2 個參數，導致 `userRole` 與 `idToken` 為 `undefined`，管理員與授權成員的免 PIN 判定失效。 | 在 `app.js` 頂層建立統一轉接函式 `isTripUnlocked(tripUuid, hasPassword)`，自動注入全域 `userRole`、`idToken` 與 `isTokenExpired(idToken)`，完美相容現有兩參數呼叫習慣。 | **通過 (單元實測＋正式程式靜態接線)** |
| **P1 URL 驗證與 HTML 轉義職責混亂** | `sanitizeUrl()` 內部誤包 `escapeAttribute()`，將 `&` 轉成 `&amp;`。導致：(1) HTML 插值二次轉義產生 `&amp;amp;`；(2) DOM 直接指派 `img.src = sanitizeUrl()` 被插入 `&amp;` 字面值，破壞 Drive 縮圖載入。 | 徹底解耦職責：`sanitizeUrl()` 僅校驗協議白名單並回傳原始乾淨 URL；HTML 屬性插值時統一外包單次 `escapeAttribute()`；DOM 屬性直接指派使用原始 URL。 | **通過 (原始 & 保留、單次跳脫與 DOM 實測)** |
| **測試名稱與範疇精準定義** | 原測試將沙盒轉接測試冠以「正式頁面整合實測」，易造成線上真實 Google 流程已全數驗收的誤解。 | 調整為「門禁轉接函式操作型單元測試＋正式程式靜態接線檢查」，誠實界定程式碼邏輯覆蓋範圍，不誇大線上登入實測。 | **通過 (12/12 項代碼邏輯檢測)** |
| **版本升級破除 CDN 快取** | 前端升級至 `20260917_12`。 | `app.js`、`index.html` 與 `trip-state.js` 均一致升級為 `20260917_12`。 | **通過 (靜態版本核對)** |
| **GAS 後端版本狀態** | 本次不涉及後端修改。 | `gas-code.js` 保持不變，本次僅需部署 GitHub Pages，無需重複發布 Apps Script。 | **確認一致 (無須重新部署 GAS)** |

---

## 二、驗收矩陣現狀（工程事實劃分）

### (A) 程式碼層級已確認通過項目

| 檢驗項目 | 檢驗方式 | 判定結果 | 說明 |
| :--- | :--- | :---: | :--- |
| **靜態語法檢查** | `node -c app.js; node -c gas-code.js; node -c trip-state.js` | **通過** | 全檔案無語法錯誤。 |
| **操作型單元與接線測試** | `node verify_v12_regression.js` | **12/12 通過** | 包含轉接函式單元實測、接線點統計、URL 職責分離、登出防回填等。 |
| **版本標籤一致性** | 原始碼比對 | **通過** | `index.html`、`app.js` 與 `trip-state.js` 均為 `20260917_12`。 |
| **門禁轉接函式邏輯** | 提取 `app.js` 真實函式於沙盒測試 | **通過** | 注入有效 admin/member 放行，訪客無 PIN 攔截，憑記憶體放行。 |
| **正式程式靜態接線** | 正則掃描 `app.js` 呼叫點 | **通過** | 8 處以上門禁呼叫點均採用轉接函式，無漏傳身分風險。 |
| **URL 原始字面值保持** | `sanitizeUrl()` 處理帶參數網址 | **通過** | 保留原始 `&`，不混入 `&amp;`。 |
| **HTML 屬性單次轉義** | `escapeAttribute(sanitizeUrl())` | **通過** | 產生單次 `&amp;`，絕無 `&amp;amp;`，引號轉為 `&quot;`。 |
| **DOM 屬性直設安全** | `img.src = sanitizeUrl()` | **通過** | 不含 `&amp;` 字面值，保護圖片請求。 |
| **未跳脫表單統計** | 正則掃描 input / textarea | **0 處遺漏** | 100% 屬性與標籤安全跳脫覆蓋。 |
| **GAS 後端狀態** | 與 `_11` 逐行核對 | **無變更** | 本次毋須重新發布 GAS 部署版本。 |

### (B) 線上真實帳號待驗收項目

| 待驗收情境 | 驗收方式 | 當前狀態 | 說明 |
| :--- | :--- | :---: | :--- |
| **1. 管理員登入免 PIN** | 點擊 Google 登入，開啟設有 PIN 的行程（如岡山） | **待線上驗收** | 確認不跳出輸入 PIN 視窗，直接進入手冊。 |
| **2. 授權成員免 PIN 與編輯** | 成員帳號登入後免 PIN 進入，修改一筆清單 | **待線上驗收** | 確認能直接進入，且勾選或儲存成功同步。 |
| **3. 未登入訪客權限邊界** | 訪客身分輸入正確 PIN 進入手冊 | **待線上驗收** | 僅能瀏覽，點擊勾選或編輯提示唯讀無法修改。 |
| **4. Google Drive 圖片與地圖載入** | 開啟含 Google Drive 照片或查詢參數之交通路線圖 | **待線上驗收** | 檢視 Network 與圖片元件，確認無 404 正常呈現。 |
| **5. 儲存中途登出防回填** | 觸發儲存瞬間立刻登出，再重新登入其他帳號 | **待線上驗收** | 確認無舊回應覆蓋新帳號資料。 |

---

## 三、如何本機復現全生命週期測試

在專案目錄下執行：
```bash
node verify_v12_regression.js
```
此測試直接透過沙盒環境與正式模組 `trip-state.js`，對正式頁面的兩參數轉接呼叫、URL 與 HTML 跳脫職責分離、登出攔截、跨行程隔離進行全量斷言。
