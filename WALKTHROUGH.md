# 旅遊手冊系統 20260917_10 回歸修復與操作型架構指引

本文件詳細記錄 `20260917_10` 版本之深層架構重構、非同步 Payload 隔離、共用狀態模組 (`trip-state.js`) 建立、全面跳脫覆蓋與真實操作型測試。

---

## 一、本次 20260917_10 修正重點詳細對照表

| 修正項目 | 前版 (dbad2a4) 實質缺陷 | 20260917_10 具體重構解法 | 驗證方式與狀態 |
| :--- | :--- | :--- | :--- |
| **P0 非同步儲存跨行程污染** | 連線等待期間若切換行程，API 成功後拿當前全域 `tripData` 更新快照，造成奧捷快照被寫入岡山 UUID。 | 連線前鎖定深拷貝 `savingPayload`，後續 API 請求與 `updateConfirmedSnapshot` 均綁定此獨立 Payload，絕不存取當前全域變數。 | **通過 (真實操作型實測)** |
| **P0 測試與正式代碼分家** | 舊測試腳本在測試檔內重寫簡化版函式，無法檢驗正式環境代碼缺陷。 | 建立 [`trip-state.js`](trip-state.js) 模組，正式網頁與測試腳本共用同一套狀態管理、快照與門禁函式。 | **通過 (CommonJS / 瀏覽器共用)** |
| **P1 表單與對話框漏跳脫** | 飯店表單 5 處跨行 input (`h.name`, `h.addr`, `h.checkin`, `h.checkout`, `h.note`) 及 `openConfirmModal` 的 `message` 未跳脫。 | 全量套用 `escapeAttribute()` 於所有 input，`openConfirmModal` 的 `message` 套用 `escapeHtml()`，全檔案未跳脫統計歸零。 | **通過 (代碼覆蓋率 100% 實測)** |
| **P1 成員分頁切換被誤鎖** | `visibilitychange` 仍檢查已淘汰之 `unlocked_trip_` 且僅排除管理員，授權成員切換瀏覽器分頁即跳出鎖定畫面。 | 移除舊旗標檢查，統一呼叫標準門禁函式 `isTripUnlocked(currentTripUuid, hasPassword)`，成員切換分頁暢行無阻。 | **通過 (真實操作型實測)** |
| **P1 登出記憶體殘留** | `logout()` 未清空 `tripData`、`currentTripUuid` 與 `confirmedSnapshots`，且未讓尚未完成的非同步回應失效。 | 補齊清空邏輯，並對 `tripRequestSequence` 與 `tripsRequestSeq` 推進流水號，使登出前發出的舊回應徹底失效。 | **通過 (靜態與生命週期審查)** |
| **P1 返回大廳行為一致性** | `showHubView()` 註解稱「清除解鎖授權」，實際與程式記憶體行為矛盾。 | 註解與邏輯統一校正為「分頁會話期間於記憶體保持解鎖，重整或關閉分頁即自動失效」，符合主流 UX。 | **通過 (代碼一致性審查)** |
| **版本升級與 CDN 破除** | 前端升級至 `20260917_10`，破除 GitHub Pages 快取。 | `app.js`、`index.html` 與 `trip-state.js` 均標記為 `20260917_10`。 | **通過 (靜態版本核對)** |

---

## 二、驗收矩陣現狀（工程事實劃分）

| 驗收維度 | 檢驗項目 | 當前狀態 | 說明 |
| :--- | :--- | :--- | :--- |
| **靜態語法檢查** | `node -c app.js; node -c gas-code.js; node -c trip-state.js` | **通過** | 無 JavaScript 語法錯誤。 |
| **操作型回歸檢測** | `node verify_v10_regression.js` | **12 項全數通過** | 直接載入正式 `trip-state.js` 進行非同步切換、失敗回滾、門禁邊界實測。 |
| **版本標籤一致性** | 檔案版本號核對 | **通過** | `index.html`、`app.js` 與 `trip-state.js` 均為 `20260917_10`。 |
| **公開端行程切換** | 岡山／奧捷行程切換與卡片顯示 | **通過** | 公開端切換正常，未再發生串頁。 |
| **管理員實體登入角色切換** | 實體帳號於瀏覽器登入 | **待使用者實測** | 需以管理員 Google 帳號實體登入，確認角色轉為 `admin` 且右上角出現後台膠囊。 |
| **授權成員免 PIN 進入手冊** | 實體帳號於瀏覽器登入 | **待使用者實測** | 將測試帳號填入主表 `allowed_users`，確認登入後免輸 PIN 直接開啟該行程。 |
| **授權成員修改 7 類內容表** | 實體帳號於瀏覽器登入 | **待使用者實測** | 成員登入後修改清單或景點，確認雲端儲存成功，且 Info 表不受影響。 |
| **管理員 PIN 保持與變更** | 實體帳號於瀏覽器登入 | **待使用者實測** | 於後台測試保持現有 PIN、修改 PIN 與取消 PIN 三種場景。 |
| **圖片上傳至 Google Drive** | 實體帳號於瀏覽器登入 | **待使用者實測** | 於前端上傳照片，確認檔案成功存入 Google Drive 且縮圖正確顯示。 |
| **線上 GAS 部署版本** | Apps Script Web App 重新發布 | **待手動部署新版本** | 本次變更為前端架構，若需啟用後端 UUID 查重與快取優化，需手動發布 GAS 新版本。 |

---

## 三、如何本機復現操作型測試

在專案目錄下執行：
```bash
node verify_v10_regression.js
```
此測試直接透過 CommonJS 引用正式模組 `trip-state.js`，執行真實資料流斷言。
