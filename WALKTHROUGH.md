# 旅遊手冊系統 20260917_08-regression 回歸修復與維護指引

本文件詳細記錄 `20260917_08-regression` 版本之安全性修正、阻斷性修復、效能架構升級及驗收矩陣。

---

## 一、本次 20260917_08 回歸修復重點

| 優先級 | 修正項目 | 修正前狀況 | 20260917_08 修正方式 | 驗證結果 |
| :--- | :--- | :--- | :--- | :--- |
| **P0** | **航班分頁阻斷錯誤** | `renderFlights()` 第 2803 行引用未定義的 `isAdmin`，拋出 `ReferenceError` 導致航班與飯店整頁無法渲染。 | 移除重複多餘的 `addHotelBtn`（標題旁已具備受 `canEdit` 保護的新增按鈕）。 | **通過 (程式驗證)** |
| **P0** | **GAS JWT 本地偽解碼漏洞** | 當 Google Tokeninfo 失敗時直接執行 `decodeJwtPayload` 本地 Base64 解碼，未校驗簽章與發行方。 | 徹底刪除 `decodeJwtPayload`，僅信任 Google 官方 Tokeninfo 驗證結果；驗證失敗直接拒絕。 | **通過 (程式驗證)** |
| **P1** | **刪除天數漏改與最後一天保護** | `deleteCurrentDay()` 仍為裸呼叫 `save()` 未等待；若天數被刪光會觸發 `save()` 攔截造成資料脫節。 | 1. 加入 `days.length <= 1` 禁止刪除最後一天之防呆；<br>2. 改為 `const ok = await save(); return ok !== false;`。 | **通過 (程式驗證)** |
| **P1** | **周遊券編輯漏改非同步等待** | `openEditTransitPassModal()` 提交時未等待 `save()` 完成即直接彈出 Toast 並關閉 Modal。 | 改為 `const ok = await save(); if (ok !== false) showToast(...); return ok !== false;`。 | **通過 (程式驗證)** |
| **P1** | **fetchTrips 請求風暴與防重複** | 頁面載入、Google 登入連鎖觸發多次重複的 `fetchTrips()`，造成 GAS 嚴重延遲。 | 實作 `inFlightTripsPromise` 與身分 Token 檢查，同身分進行中請求直接共用 Promise，不打第二遍 GAS。 | **跳過重複連線** |
| **P1** | **登入過渡狀態提示** | 點擊 Google 登入後，後端驗證前曾短暫顯示「訪客已登入」，易引發誤解。 | 登入後立即設為 `userRole = "verifying"`，界面顯示「⏳ 正在驗證帳號權限...」，絕不閃退或誤顯訪客。 | **通過 (程式驗證)** |
| **P1** | **整合 bootstrap 合併初始化端點** | 原登入流程先打 `getTrips` 再打 `getTripData`，受 GAS 冷啟動影響耗時倍增。 | GAS 新增 `action=bootstrap` 端點，單次請求一次回傳角色、行程清單與當前行程手冊資料。 | **通過 (程式驗證)** |
| **P1** | **GAS Token 驗證快取** | 每次 API 操作皆重複連線 Google Tokeninfo 驗證，拉長回應時間。 | 依 Token 之 SHA-256 雜湊鍵在 `CacheService` 暫存 Email 300 秒，大幅降低驗證開銷。 | **通過 (程式驗證)** |
| **P1** | **儲存失敗畫面還原機制** | `save()` 失敗或斷網時，前端畫面仍呈現修改後狀態，造成假性成功。 | `save()` 寫入前保留 `backupSnapshot` 深拷貝，寫入失敗時精準還原記憶體與畫面。 | **通過 (程式驗證)** |
| **P2** | **行程建立 UUID 查重** | 建立行程時未檢查 UUID 是否重複，可能導致多筆紀錄互相覆蓋。 | `createTrip` 寫入前主動掃描主控表 UUID，重複時直接拋出錯誤並拒絕建立。 | **通過 (程式驗證)** |
| **P2** | **子表初始化原子性保證** | 若子表初始化出錯，主控表已寫入新列，產生孤立紀錄。 | 先確保 `initializeSubSheet` 成功建立各工作表，最後才向 Trips 主表寫入紀錄。 | **通過 (程式驗證)** |
| **P2** | **HTML 屬性安全跳脫** | 表單 input 缺少全面引號跳脫，若資料含雙引號可能破壞屬性邊界。 | 引入 `escapeAttribute()` 輔助函式，跳脫 `&`, `"`, `'`, `<`, `>` 等符號。 | **通過 (程式驗證)** |

---

## 二、三級權限與門禁設計規格

| 身分角色 | 大廳總覽 | 閱讀手冊 | 7 類內容表修改 | 核心設定管理 (PIN / 成員) |
| :--- | :--- | :--- | :--- | :--- |
| **未登入訪客** | 可見全部行程摘要（無密碼） | 須輸入 PIN 解鎖 | 唯讀模式（全面鎖定） | 無權限 |
| **PIN 訪客** | 可見全部行程摘要 | 輸入正確 PIN 後放行 | 唯讀模式（全面鎖定） | 無權限 |
| **授權成員** | 可見全部行程摘要 | **免輸入 PIN 直接開啟** | **可編輯 7 類內容表**（不碰 Info 表） | 物理隔離（無權限） |
| **系統管理員** | 可見全部行程摘要 | **免輸入 PIN 直接開啟** | **可編輯 7 類內容表** | **可管理**（具 PIN 保持防呆） |

---

## 三、驗收矩陣：程式靜態驗證 vs. 實體帳號實測

由於本地環境與公開 API 無法持有管理員/成員之有效 Google 登入 Session，依工程事實嚴格劃分驗收範圍：

| 驗收項目 | 驗收類型 | 目前狀態 | 說明 |
| :--- | :--- | :--- | :--- |
| **JS / GAS 語法完整性** | 靜態程式驗證 | **通過** | `node -c app.js` 與 `node -c gas-code.js` 均無語法錯誤。 |
| **航班分頁渲染** | 靜態程式驗證 | **通過** | 刪除未定義之 `isAdmin`，模擬呼叫 `renderFlights()` 正常執行。 |
| **10 處刪除 await save()** | 靜態程式驗證 | **通過** | 包含天數刪除、活動、住宿、交通等 10 處皆等待儲存成功後才關閉視窗。 |
| **最後一天刪除防呆** | 靜態程式驗證 | **通過** | `days.length <= 1` 時阻擋刪除並彈出提示，防止資料庫脫節。 |
| **周遊券更新 await save()** | 靜態程式驗證 | **通過** | 等待雲端回傳成功後才發布 Toast 並關閉 Modal。 |
| **GAS JWT 偽解碼安全修復** | 靜態程式驗證 | **通過** | 完全移除 `decodeJwtPayload`，僅透過官方 Tokeninfo 端點驗證。 |
| **GAS 伺服器快取** | 靜態程式驗證 | **通過** | 實作 SHA-256 雜湊與 300 秒 `CacheService`。 |
| **bootstrap 合併請求端點** | 靜態程式驗證 | **通過** | GAS 端點與前端呼叫皆已實裝。 |
| **UUID 查重與子表原子初始化** | 靜態程式驗證 | **通過** | `createTrip` 查重與順序調整完成。 |
| **管理員實體登入角色切換** | 實體帳號實測 | **待使用者登入驗收** | 需使用實際管理員帳號於瀏覽器登入，確認角色轉為 `admin` 且後台按鈕顯示。 |
| **授權成員免 PIN 進入手冊** | 實體帳號實測 | **待使用者登入驗收** | 將測試帳號填入主表 `allowed_users`，登入後進入設密碼行程，確認免輸 PIN 直接開啟。 |
| **授權成員修改 7 類內容表** | 實體帳號實測 | **待使用者登入驗收** | 成員登入後修改清單或景點，確認雲端儲存成功，且 Info 表不受影響。 |
| **管理員 PIN 保持與變更** | 實體帳號實測 | **待使用者登入驗收** | 於後台測試保持現有 PIN、修改 PIN 與取消 PIN 三種場景。 |
| **圖片上傳至 Google Drive** | 實體帳號實測 | **待使用者登入驗收** | 於前端上傳照片，確認檔案寫入 Google Drive 且取得正確公開網址。 |
