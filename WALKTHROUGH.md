# 旅遊手冊系統 20260917_12 正式整合修復與架構驗收指引

本文件詳細記錄 `20260917_12` 版本之核心修正：建立統一門禁轉接函式解決 P0 參數缺漏、分離 URL 協議白名單與 HTML 屬性轉義職責解決 P1 圖片網址破壞、整合型兩參數呼叫回歸測試與線上部署指引。

---

## 一、本次 20260917_12 修正重點詳細對照表

| 修正項目 | 前版 (21ad0cd / _11) 實質缺陷 | 20260917_12 具體重構解法 | 驗證方式與狀態 |
| :--- | :--- | :--- | :--- |
| **P0 門禁整合接線參數缺漏** | `trip-state.js` 模組函式需要 5 個參數，但 `app.js` 正式頁面 8 處呼叫點均只傳 2 個參數，導致 `userRole` 與 `idToken` 為 `undefined`，管理員與授權成員的免 PIN 判定失效。 | 在 `app.js` 頂層建立統一轉接函式 `isTripUnlocked(tripUuid, hasPassword)`，自動注入全域 `userRole`、`idToken` 與 `isTokenExpired(idToken)`，完美相容現有呼叫慣例。 | **通過 (正式頁面兩參數整合實測)** |
| **P1 URL 驗證與 HTML 轉義職責混亂** | `sanitizeUrl()` 內部誤包 `escapeAttribute()`，將 `&` 轉成 `&amp;`。導致：(1) HTML 插值二次轉義產生 `&amp;amp;`；(2) DOM 直接指派 `img.src = sanitizeUrl()` 被插入 `&amp;` 字面值，破壞 Drive 縮圖載入。 | 徹底解耦職責：`sanitizeUrl()` 僅校驗協議白名單並回傳原始乾淨 URL；HTML 屬性插值時統一外包單次 `escapeAttribute()`；DOM 屬性直接指派使用原始 URL。 | **通過 (原始 & 保留、單次跳脫與 DOM 實測)** |
| **測試覆蓋真實整合呼叫** | 原測試套件直接傳入 5 個參數呼叫模組，造成「單元測試通過，正式頁面整合失敗」的假象，且將 `&amp;` 視為正確輸出。 | [`verify_v12_regression.js`](verify_v12_regression.js) 直接模擬正式頁面以 2 個參數呼叫轉接函式，並對 URL 原始字面值、單次轉義與 DOM 安全進行嚴格斷言。 | **通過 (12/12 項整合實測)** |
| **版本升級破除 CDN 快取** | 前端升級至 `20260917_12`。 | `app.js`、`index.html` 與 `trip-state.js` 均一致升級為 `20260917_12`。 | **通過 (靜態版本核對)** |
| **GAS 後端版本狀態** | 本次不涉及後端修改。 | `gas-code.js` 保持不變，本次僅需重新部署 GitHub Pages，無需重複發布 Apps Script。 | **確認一致 (無須重新部署 GAS)** |

---

## 二、驗收矩陣現狀（工程事實劃分）

| 驗收維度 | 檢驗項目 | 當前狀態 | 說明 |
| :--- | :--- | :--- | :--- |
| **靜態語法檢查** | `node -c app.js; node -c gas-code.js; node -c trip-state.js` | **通過** | 無 JavaScript 語法錯誤。 |
| **操作型回歸檢測** | `node verify_v12_regression.js` | **12 項全數通過** | 包含頁面兩參數呼叫接線實測、URL 職責分離實測、登出世代防回填等。 |
| **版本標籤一致性** | 檔案版本號核對 | **通過** | `index.html`、`app.js` 與 `trip-state.js` 均為 `20260917_12`。 |
| **管理員免 PIN 判定** | 正式 2 參數呼叫 `isTripUnlocked("admin_trip", true)` | **通過 (整合實測)** | 轉接函式自動代入當前 `userRole` 與有效 `idToken`，直接免密放行。 |
| **授權成員免 PIN 判定** | 正式 2 參數呼叫 `isTripUnlocked("member_trip", true)` | **通過 (整合實測)** | 轉接函式自動判定 `tripPermissions` 中的 `canEdit`，直接免密放行。 |
| **訪客 PIN 保護與解鎖** | 正式 2 參數呼叫訪客未解鎖／已解鎖情境 | **通過 (整合實測)** | 未解鎖訪客嚴格攔截；輸入正確 PIN 憑記憶體 Map 順利放行。 |
| **URL 原始字面值保持** | `sanitizeUrl()` 處理帶參數網址 | **通過 (字串實測)** | 保留原始 `&`，不混入 `&amp;`，保證網路請求正常解析查詢參數。 |
| **HTML 屬性單次轉義** | `escapeAttribute(sanitizeUrl())` | **通過 (字串實測)** | 產生單次 `&amp;`，絕無 `&amp;amp;` 雙重轉義，同時將 `"` 轉為 `&quot;` 防屬性逃逸。 |
| **DOM 屬性直設安全** | `img.src = sanitizeUrl()` | **通過 (字串實測)** | 不含 `&amp;` 字面值，Google Drive 圖片與縮圖可順利載入。 |
| **未跳脫表單統計** | 正則掃描 input / textarea | **0 處遺漏** | 達成 100% 屬性與標籤安全跳脫覆蓋。 |
| **管理員實體登入角色切換** | 實體帳號於瀏覽器登入 | **待使用者線上實測** | 需以管理員 Google 帳號實體登入，確認角色轉為 `admin` 且右上角出現後台膠囊。 |
| **授權成員修改 7 類內容表** | 實體帳號於瀏覽器登入 | **待使用者線上實測** | 成員登入後修改清單或景點，確認雲端儲存成功，且 Info 表不受影響。 |
| **線上 GAS 部署版本** | Apps Script Web App 重新發布 | **本次無變更** | 本次 Commit 專注於前端架構與安全，`gas-code.js` 無變動。 |

---

## 三、如何本機復現全生命週期測試

在專案目錄下執行：
```bash
node verify_v12_regression.js
```
此測試直接透過沙盒環境與正式模組 `trip-state.js`，對正式頁面的兩參數轉接呼叫、URL 與 HTML 跳脫職責分離、登出攔截、跨行程隔離進行全量斷言。
