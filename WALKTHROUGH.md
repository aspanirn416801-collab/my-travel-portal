# 旅遊手冊系統 20260917_07 驗收與維護指引

本文件詳細記錄 20260917_07 版本之架構優化、安全修正與後續登入驗收指引。

---

## 一、本次 20260917_07 修正重點

| 項目 | 修正前狀況 | 20260917_07 修正方式 |
| :--- | :--- | :--- |
| **刪除確認視窗非同步等待** | 清單、飯店、天數、景點、美食、代購及交通刪除中，未等待 `save()` 完成即關閉 Modal。 | 1. `openConfirmModal` 處理函式支援 `await onConfirm()` 並檢查回傳值；<br>2. 10 處刪除與天數重編一律改為 `const ok = await save(); return ok !== false;`，確保雲端完成後才關閉視窗。 |
| **getTripMeta 主題設定** | `getTripMeta` 未自 Info 表讀取 `Theme`，且前端預設為 `"violet"`，可能將自訂主題覆蓋。 | 1. GAS 的 `getTripMeta` 精準開啟 Info 表讀取 `Theme`；<br>2. 前端預設「自動適配」保持為空字串 `""`，不強制覆蓋為特定主題色。 |
| **快取版本號** | 舊版快取標籤為 `20260917_06`。 | `app.js` 與 `index.html` 統一更新為 `20260917_07`。 |

---

## 二、三級權限與門禁設計規格

| 身分角色 | 大廳總覽 | 閱讀手冊 | 7 類內容表修改 | 核心設定管理 (PIN / 成員) |
| :--- | :--- | :--- | :--- | :--- |
| **未登入訪客** | 可見全部行程摘要（無密碼） | 須輸入 PIN 解鎖 | 唯讀模式（全面鎖定） | 無權限 |
| **PIN 訪客** | 可見全部行程摘要 | 輸入正確 PIN 後放行 | 唯讀模式（全面鎖定） | 無權限 |
| **授權成員** | 可見全部行程摘要 | **免輸入 PIN 直接開啟** | **可編輯 7 類內容表**（不碰 Info 表） | 物理隔離（無權限） |
| **系統管理員** | 可見全部行程摘要 | **免輸入 PIN 直接開啟** | **可編輯 7 類內容表** | **可管理**（具 PIN 保持防呆） |

---

## 三、待登入實測驗收項目說明

由於公開端 API 測試無法模擬需要有效 Google ID Token 的寫入與管理端點，後續建議以實體 Google 帳號進行以下場景之驗收：

1. **授權成員實際寫入驗收**：
   - 於主控表指定行程之第 5 欄填入成員 Google Email。
   - 使用該成員帳號登入前端，點擊該行程（確認免輸 PIN 直接開）。
   - 勾選清單、修改景點時段、新增美食，確認雲端 7 類內容表成功同步寫入，且 Info 表資料未被變動。
2. **成員上傳圖片驗收**：
   - 成員登入狀態下，於景點或美食編輯彈窗上傳照片，確認可成功儲存並取得直連縮圖。
3. **管理員 PIN 三種操作驗收**：
   - 管理員登入後進入後台，點選「編輯設定」。
   - 測試 (A) 保持現有密碼更新名稱、(B) 設定新密碼、(C) 取消密碼保護，確認主控表與 Info 表之 PIN 欄位變化正確。
4. **行動裝置鍵盤適配**：
   - 於手機瀏覽器開啟編輯彈窗，輸入資料時確認虛擬鍵盤彈起時畫面不位移或跑版。
