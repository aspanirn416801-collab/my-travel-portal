# 旅遊手冊系統 20260918_13 局部優化驗收指引與全生命週期操作手冊

本文件詳細記錄 `20260918_13` 版本之 4 項核心優化：身分雙軌化（`authStatus` 顯示軌 vs `verifiedRole` 實質權限軌分離）、PIN 鎖定手冊提前請求阻斷與端點分流、GAS 三層原子 CacheService 前置於 `openById` 之前與權限版本號管理、空日期三層 Fallback 與稱謂校正，以及完整的前後端雙步部署指引。

---

## 一、本次 20260918_13 修正重點詳細對照表

| 修正項目 | 前版 (e529801 / _12) 實質現象 | 20260918_13 具體重構解法 | 驗證方式與狀態 |
| :--- | :--- | :--- | :--- |
| **P1 身分恢復雙軌分離** | 重整時先顯示「訪客已登入」長達 20 多秒，GAS 逾時則直接打回訪客模式。 | 建立雙軌架構：`authStatus` 顯示「⏳ 正在恢復身分...」（搭配 `auth_role_hint`）；實質權限 `verifiedRole` 嚴格依本次 GAS 回傳更新；逾時轉為 `auth-error`，絕不清除有效 Token。 | **通過 (雙軌狀態轉換實測)** |
| **P1 PIN 鎖定提前請求阻斷** | 進入鎖定行程時提前呼叫 `fetchTripData()`，bootstrap 沒手冊又補發，造成並發風暴且舊回應有回鎖風險。 | (1) `navigateTo()` 鎖定分支直接 `return;`，未解鎖前 100% 阻斷手冊請求。<br>(2) 未登入訪客進 PIN 行程發送 `getTrips` 享受快取，有 Token 才發 `bootstrap`。<br>(3) 送出 PIN 帶入雙重流水號防舊回應回鎖。 | **通過 (靜態阻斷與端點分流實測)** |
| **GAS 三層 CacheService 前置** | 每次請求均強制開啟試算表 (`openById`)，訪客亦需等待 20~25 秒。 | 快取檢查前置於 `SpreadsheetApp.openById()` 之前：<br>(1) 公開大廳 `public_trips_v1`（快取 600s）。<br>(2) 帳號權限 `access_<hash>_<rev>`（300s，`ScriptProperties` + `LockService` 併發鎖）。<br>(3) 手冊內容 `trip_content_<uuid>`（300s，90KB 容量保護）。 | **通過 (快取前置與原子組裝實測)** |
| **完整快取失效矩陣** | 各端點寫入後未明確指定清除目標，可能造成資料或權限不同步。 | 嚴格落實**累加清除**（絕不用互斥 `else if`）：<br>• `createTrip`：清除 `public_trips_v1`、遞增 `ACCESS_REVISION`。<br>• `updateTripData` / 圖片：清除 `trip_content_<uuid>`。<br>• `updateTripMeta`：名稱/日期清除公表與手冊；PIN 清除公表；成員清單遞增版本號。 | **通過 (累加失效矩陣審查)** |
| **空日期三層 Fallback** | 試算表日期空白時，API 回傳空字串覆蓋首頁預設公開摘要。 | 前端套用三層 Fallback：`t.startDate \|\| existing?.startDate \|\| fallback?.startDate \|\| ""`，徹底杜絕天數消失。 | **通過 (三層結構比對實測)** |
| **未授權帳號稱謂校正** | 只要持有效 Google Token 均顯示為「團員」，即使無任何行程編輯權。 | 判定所有行程之 `canEdit`，全無權限者顯示「👤 已登入 (未授權)」，有權限才稱呼「👤 團員」。 | **通過 (字串邏輯審查)** |

---

## 二、20260918_13 操作型回歸測試結果 (12/12 通過)

在專案目錄下執行：
```bash
node verify_v13_regression.js
```

| 項次 | 檢驗指標 | 檢測內容與判定標準 | 檢測結果 |
| :---: | :--- | :--- | :---: |
| 1 | **版本標籤一致性** | `app.js`、`index.html`、`trip-state.js` 均為 `20260918_13` | **通過** |
| 2 | **身分雙軌化審查** | `authStatus` 控制顯示，`verifiedRole` 綁定本次後端回傳，杜絕本地假性提權 | **通過** |
| 3 | **門禁轉接單元實測** | 正式呼叫點扣除定義後精確等於 9 處，管理員/成員/PIN 放行邏輯正確 | **通過** |
| 4 | **PIN 鎖定嚴格阻斷** | 鎖定分支直接 `return` 絕無手冊請求，PIN 提交具備雙重流水號防護 | **通過** |
| 5 | **端點選擇分流** | 未登入訪客進 PIN 行程改發 `getTrips` 命中快取，有 Token 才發 `bootstrap` | **通過** |
| 6 | **空日期三層 Fallback** | 依序由後端值 ➔ 既有快取值 ➔ 預設公開摘要自動填補，防空白覆蓋 | **通過** |
| 7 | **GAS 快取前置審查** | 快取檢查位置 index 10061 < 開表位置 index 11196，命中時 100% 避免開表 | **通過** |
| 8 | **ScriptProperties 持久化** | 權限版本號存放於 ScriptProperties，遞增採 10 秒 LockService 原子鎖 | **通過** |
| 9 | **90KB 容量安全保護** | `safePutCache` 檢查位元組 `< 90000`，超量手冊安全略過快取改為試算表直讀 | **通過** |
| 10 | **完整快取失效矩陣** | `updateTripMeta` 支援各欄位累加判定失效，新增行程遞增版本號 | **通過** |
| 11 | **未授權帳號文字校正** | 無任何行程 `canEdit` 權限之 Google 帳號顯示為「已登入 (未授權)」 | **通過** |
| 12 | **登出世代防舊回應回填** | 登出時遞增世代號阻斷延遲非同步回應重建快照與 Session | **通過** |

---

## 三、兩步部署操作指南

本次升級涉及前端程式與後端 GAS，發布時必須分別完成兩步部署：

### 第一步：GitHub Pages 前端部署
1. 本地程式碼已通過全量回歸測試。
2. 執行 `git push origin main` 將 `_13` 版本推送到 GitHub。
3. GitHub Pages 自動建置完成後，正式線上將同步載入 `_13`。

### 第二步：Google Apps Script 重新部署 Web App
1. 開啟您的 Google Apps Script 專案編輯器。
2. 將本專案中的 [`gas-code.js`](./gas-code.js) 完整內容複製，覆蓋 Apps Script 中的程式碼。
3. 點擊右上角 **「部署」** ➔ **「管理部署作業」**。
4. 點選現有的 Web App 部署項目，點擊鉛筆圖示（編輯）。
5. 在「版本」下拉選單中選擇 **「建立新版本」**（版本說明可填：`20260918_13 快取優化與權限版本號`）。
6. 點擊 **「部署」** 儲存發布。
   > [!IMPORTANT]
   > 必須選擇「建立新版本」並點擊部署，GAS 網頁應用程式才會真正運行最新的快取前置與原子組裝邏輯！
