# 旅遊手冊系統 20260917_11 回歸修復與操作型架構指引

本文件詳細記錄 `20260917_11` 版本之核心架構統一、狀態模組唯一源頭整合、登出非同步阻斷、URL 屬性安全跳脫與全生命週期操作型測試。

---

## 一、本次 20260917_11 修正重點詳細對照表

| 修正項目 | 前版 (bc3a1da) 實質缺陷 | 20260917_11 具體重構解法 | 驗證方式與狀態 |
| :--- | :--- | :--- | :--- |
| **P0 狀態模組唯一源頭** | `app.js` 本地仍重複定義 8 個 Map 與函式，正式網站未真正使用 `trip-state.js`，測試與上線脫節。 | 徹底刪除 `app.js` 本地重複宣告，統一解構引用 `trip-state.js`；正式網站與測試套件 100% 操作同一套 Map 與函式。 | **通過 (靜態零重複審查)** |
| **P0 登出非同步回填漏洞** | 儲存 API 送出後若使用者登出，舊儲存成功回傳時仍調用快照更新，敏感手冊被重新寫回 Session。 | 引入 `authGeneration` 登入世代計數器，登出時遞增；儲存回應前比對世代，若已登出或身分切換則直接作廢丟棄。 | **通過 (真實生命週期實測)** |
| **P1 URL 屬性引號穿透** | `sanitizeUrl()` 僅驗證協議白名單，未處理引號；路線圖 `currentImg` 未跳脫，可能破壞 `src` 邊界。 | `sanitizeUrl()` 內部整合 `escapeAttribute()`，強制跳脫引號；全面防護所有 `src` 與 `href` 屬性。 | **通過 (引號防穿透實測)** |
| **P1 表單與對話框跳脫覆蓋** | 飯店跨行 input 與 `openConfirmModal` 訊息跳脫前版曾有漏網項目。 | 補齊飯店跨行 input 與確認視窗內文轉義，全檔案未跳脫 input / textarea 統計數確認為 0。 | **通過 (覆蓋率 100% 檢查)** |
| **測試覆蓋真實生命週期** | 舊測試僅單純呼叫快照函式，未模擬「儲存中途使用者登出」的非同步競態流程。 | [`verify_v11_regression.js`](verify_v11_regression.js) 直接載入正式代碼，完整模擬發起儲存、中途登出與舊回應攔截。 | **通過 (12/12 項操作實測)** |
| **版本升級破除 CDN 快取** | 前端升級至 `20260917_11`。 | `app.js`、`index.html` 與 `trip-state.js` 均升級為 `20260917_11`。 | **通過 (靜態版本核對)** |

---

## 二、驗收矩陣現狀（工程事實劃分）

| 驗收維度 | 檢驗項目 | 當前狀態 | 說明 |
| :--- | :--- | :--- | :--- |
| **靜態語法檢查** | `node -c app.js; node -c gas-code.js; node -c trip-state.js` | **通過** | 無 JavaScript 語法錯誤。 |
| **操作型回歸檢測** | `node verify_v11_regression.js` | **12 項全數通過** | 包含重複宣告審查、登出非同步阻斷實測、URL 引號跳脫實測等。 |
| **版本標籤一致性** | 檔案版本號核對 | **通過** | `index.html`、`app.js` 與 `trip-state.js` 均為 `20260917_11`。 |
| **未跳脫表單統計** | 正則掃描 input / textarea / URL | **0 處遺漏** | 達成 100% 屬性與標籤安全跳脫覆蓋。 |
| **公開端行程切換** | 岡山／奧捷行程切換與卡片顯示 | **通過** | 公開端切換正常，未再發生串頁。 |
| **管理員實體登入角色切換** | 實體帳號於瀏覽器登入 | **待使用者實測** | 需以管理員 Google 帳號實體登入，確認角色轉為 `admin` 且右上角出現後台膠囊。 |
| **授權成員免 PIN 進入手冊** | 實體帳號於瀏覽器登入 | **待使用者實測** | 將測試帳號填入主表 `allowed_users`，確認登入後免輸 PIN 直接開啟該行程。 |
| **授權成員修改 7 類內容表** | 實體帳號於瀏覽器登入 | **待使用者實測** | 成員登入後修改清單或景點，確認雲端儲存成功，且 Info 表不受影響。 |
| **管理員 PIN 保持與變更** | 實體帳號於瀏覽器登入 | **待使用者實測** | 於後台測試保持現有 PIN、修改 PIN 與取消 PIN 三種場景。 |
| **圖片上傳至 Google Drive** | 實體帳號於瀏覽器登入 | **待使用者實測** | 於前端上傳照片，確認檔案成功存入 Google Drive 且縮圖正確顯示。 |
| **線上 GAS 部署版本** | Apps Script Web App 重新發布 | **本次無變更** | 本次 Commit 專注於前端架構與安全，`gas-code.js` 無變動。 |

---

## 三、如何本機復現全生命週期測試

在專案目錄下執行：
```bash
node verify_v11_regression.js
```
此測試直接透過 CommonJS 引用正式模組 `trip-state.js`，對登出攔截、跨行程隔離、URL 引號轉義與 app.js 零重複定義進行嚴格斷言。
